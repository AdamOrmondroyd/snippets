{
    "uv install and pytest": {
        "prefix": ["uvci"],
        "body": [
            "name: lint and test",
            "",
            "on:",
            "  push:",
            "    branches: [ ${1|main,master|} ]",
            "  pull_request:",
            "",
            "jobs:",
            "  lint:",
            "    runs-on: ubuntu-latest",
            "    steps:",
            "    - uses: actions/checkout@v7",
            "    - uses: astral-sh/setup-uv@v8.2.0",
            "    - run: uvx ruff check",
            "",
            "  test:",
            "    runs-on: ubuntu-latest",
            "    strategy:",
            "      matrix:",
            "        python-version: [\"${2:3.14}\"]",
            "",
            "    steps:",
            "    - uses: actions/checkout@v7",
            "      with:",
            "        fetch-depth: 0  # full history so setuptools_scm can derive the version",
            "    - name: Install uv and set up python",
            "      uses: astral-sh/setup-uv@v8.2.0",
            "      with:",
            "        python-version: \\${{ matrix.python-version }}",
            "    - name: Install the project",
            "      run: uv sync --locked",
            "    - name: Test with pytest",
            "      run: uv run pytest"
        ],
        "description": "GitHub Actions workflow with separate lint and test jobs: uvx ruff check on a bare checkout, and uv sync --locked + pytest across a python matrix. pull_request is left unfiltered on purpose: its branches: filter matches the base branch, so a filter would skip every PR in a stack above the bottom one."
    }
}
